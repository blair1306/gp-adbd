<dec f='codebrowser/bootable/recovery/bootloader.h' l='60' type='char [32]'/>
<offset>6656</offset>
<doc f='codebrowser/bootable/recovery/bootloader.h' l='55'>// The &apos;recovery&apos; field used to be 1024 bytes.  It has only ever
    // been used to store the recovery command line, so 768 bytes
    // should be plenty.  We carve off the last 256 bytes to store the
    // stage string (for multistage packages) and possible future
    // expansion.</doc>
